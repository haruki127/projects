# システム構成

## ディレクトリ構成
```
D:\app_road\
├── app.py              # Flask バックエンド（LAS処理・API）
├── index.html          # フロントエンド（ファイルアップロード＋地図表示）
├── requirements.txt    # Python依存ライブラリ
├── rule.md             # プロジェクトルール
├── 仕様.md             # システム仕様書
├── 構成.md             # 本ファイル
└── 変更履歴.md         # 変更履歴
```

## アーキテクチャ
```
ブラウザ (index.html)
  │  ← multipart/form-data (before.las, after.las)
  ▼
Flask サーバー (app.py)
  │  laspy で LAS 読み込み
  │  numpy でグリッド計算
  │  ΔZ から危険度算出
  │  ← GeoJSON レスポンス
  ▼
Leaflet.js マップに危険度ポイントを描画
```

## API エンドポイント
| メソッド | パス | 説明 |
|---------|------|------|
| GET | / | index.html を返す |
| POST | /analyze | LAS2ファイルを受け取り GeoJSON を返す |

## GitHub
リポジトリ: https://github.com/haruki127/projects  
フォルダ: tengun/
