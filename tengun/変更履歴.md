# 変更履歴

## 2026-06-20 (2)
### アップロード進捗バーの追加
- `index.html`: プログレスバーのCSS・HTML要素を追加
- `index.html`: `fetch` → `XMLHttpRequest` に変更してアップロード進捗を取得
- `index.html`: `setProgress` 関数を追加（バー幅・パーセント表示の更新）

## 2026-06-20
### 非同期ジョブ処理への変更（通信エラー対策）
- `app.py`: `threading`, `uuid`, `shutil` を追加インポート
- `app.py`: `jobs` グローバル辞書を追加（ジョブ状態管理）
- `app.py`: `run_job` 関数を追加（バックグラウンドスレッドで解析を実行、完了後に一時ファイルを削除）
- `app.py`: `/analyze` エンドポイントをジョブ登録のみに変更（即座に `job_id` を返す）
- `app.py`: `/status/<job_id>` エンドポイントを追加（処理状態・結果を返す）
- `index.html`: `runAnalyze` を2ステップ方式に変更（POST→job_id取得 → 2秒ポーリング）
- `index.html`: `renderResult` 関数を分離

## 2026-06-18 (4)
### 一時ファイル保存先をD:に変更
- `app.py`: `TemporaryDirectory(dir="D:\\")` に変更（C:ドライブ容量不足エラー回避）

## 2026-06-18 (3)
### Windowsファイルハンドルエラー修正
- `app.py`: `laspy.read()` → `laspy.open()` コンテキストマネージャに変更（ファイルハンドルを確実に解放）
- `app.py`: `TemporaryDirectory(ignore_cleanup_errors=True)` に変更（Windows環境でのPermissionError回避）

## 2026-06-18 (2)
### 複数ファイル選択対応
- `index.html`: 災害前・後の入力に `multiple` 属性を追加、全ファイルを FormData に追加するよう変更
- `app.py`: `load_xyz` を複数パス対応に変更（結合）、`analyze_route` で `getlist` により複数ファイルを受け取るよう変更

## 2026-06-18
### 初期構築
- `仕様.md` 作成（システム仕様書）
- `構成.md` 作成（ディレクトリ・アーキテクチャ構成）
- `変更履歴.md` 作成（本ファイル）
- `app.py` 作成（Flask バックエンド：LAS読み込み・グリッド解析・GeoJSON出力）
- `index.html` 作成（フロントエンド：ファイルアップロード・Leaflet地図表示）
- `requirements.txt` 作成（依存ライブラリ一覧）
