# 手動実行ガイド — 自分の PC でパイプラインを動かす

このシステムを Cowork や Claude 経由ではなく、自分の PowerShell から直接
動かす手順です。Windows 11 + Python 3.10 以上が前提です。

**プロジェクトの場所**: `D:\projects\gensai\las_diff_pipeline\`

ドライブと大文字小文字に注意してください。`D:\Projects` ではなく `D:\projects`、
プロジェクトフォルダは `gensai`、その中に `las_diff_pipeline` があります。

```
D:\projects\
└── gensai\
    ├── .gitignore
    ├── 手動実行ガイド.md        ← 本ファイル
    └── las_diff_pipeline\
        ├── main.py
        ├── config.example.yaml
        ├── requirements.txt
        ├── README.md
        ├── pipeline\           # コアモジュール
        ├── examples\           # 合成データ / 実データ用スクリプト
        └── viewer\             # ブラウザ用 HTML
```

---

## STEP 0. 前提確認

Python が入っているか確認:

```powershell
python --version
```

→ `Python 3.10.x` 以上が出れば OK。出ない場合は <https://www.python.org/downloads/>
からインストールしてください（**「Add python.exe to PATH」にチェック必須**）。

---

## STEP 1. プロジェクトフォルダに移動

```powershell
cd D:\projects\gensai\las_diff_pipeline
```

ここを **カレントディレクトリ** にしておくと以下のコマンドがそのまま使えます。

---

## STEP 2. 依存ライブラリのインストール

### 2-1. 仮想環境を作る（プロジェクト専用の Python 環境）

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

PowerShell で「スクリプトの実行が無効」エラーが出る場合:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

を一度だけ実行し、再度 `Activate.ps1` を試してください。
仮想環境を有効化すると、プロンプトの先頭に `(.venv)` が表示されます。

### 2-2. パッケージインストール

```powershell
python -m pip install --upgrade pip
pip install -r requirements.txt
```

数分かかります。Open3D が入らない場合は `requirements.txt` の `open3d>=0.18`
を一時的にコメントアウト（`#`）して再実行してください。ICP は **scipy 自前
実装が自動でフォールバック**するので Open3D 無しでも全機能使えます。

---

## STEP 3. 動作確認（合成データ）

実データを触る前に、まず合成データで全工程が動くことを確認します。

### 3-1. 合成 LAS と建物 GeoJSON を生成

```powershell
python examples\generate_synthetic.py
python examples\generate_synthetic_buildings.py
```

→ `examples\data\` に以下ができます:
- `pre_synthetic.las` / `post_synthetic.las` (各 ~10MB)
- `buildings_synthetic.geojson` (5 棟分の矩形)

### 3-2. グリッド集約版のパイプライン実行

```powershell
python main.py --config examples\config_synthetic_no_open3d.yaml -v
```

→ `examples\outputs\synthetic\damage_score.geojson` が生成されます (2,500 セル)。

### 3-3. 建物単位スコアリング版

```powershell
python main.py --config examples\config_synthetic_building.yaml -v
```

→ `examples\outputs\synthetic_building\damage_score.geojson` が生成されます。
5 建物 (B0〜B4) のスコアが期待通り (0, 3, 3, 1, 0) になっていれば成功です。

---

## STEP 4. マップで結果を見る (API キー不要)

ブラウザは `file://` で GeoJSON を fetch できないので、ローカル HTTP サーバ
を立てます。

### 4-1. サーバ起動

```powershell
cd D:\projects\gensai\las_diff_pipeline
python -m http.server 8000
```

ターミナルに `Serving HTTP on 0.0.0.0 port 8000 ...` と出れば OK。
**このターミナルは閉じない**でください（閉じるとサーバ停止）。

### 4-2. ブラウザで開く

別タブで開く（API キー不要・登録不要）:

```
http://localhost:8000/viewer/index_osm.html
```

自動で被害マップが表示されます。表示されない場合は右パネルの
「GeoJSON ファイル」ボタンから手動選択してください。

### 4-3. ベース地図切替

右パネル「ベース地図」で 3 種類切り替え可能:

- **OpenStreetMap** — 道路・地形の世界標準
- **地理院標準** — 日本国土地理院公式タイル
- **地理院シームレス空中写真** — 衛星写真の代替（**能登半島の被害確認に推奨**）

### 4-4. 操作

- フィルタ: `≥1` / `≥2` / `3のみ` でスコア別表示
- 不透明度スライダ: 背景写真とのバランス調整
- セルクリック: dz_mean, dz_p95 などの詳細表示

---

## STEP 5. 能登半島の実データで動かす

### 5-1. 用意するデータ

| 種別 | 既定パス | 内容 |
|---|---|---|
| PRE | `D:\notowest14\07FD2032.las` | 被災前 (2024-01-19) 全点 |
| POST | `D:\ground_data_07fd1_2025\07fd203_grd.las` | 被災後 (2024-09-29) 地表のみ |

データのパスが違う場合は `examples\run_realdata.py` の先頭を編集:

```python
PRE     = r"D:\path\to\your_pre.las"
POST    = r"D:\path\to\your_post.las"
OUT_DIR = r"D:\projects\gensai\las_diff_pipeline\examples\outputs\realdata"
EPSG_WORK = 6675   # データの座標系に合わせる
```

### 5-2. 実行

```powershell
python examples\run_realdata.py
```

約 20 秒で完了し、`examples\outputs\realdata\damage_score.geojson` が
生成されます (約 28,000 セル、輪島市門前町付近)。

### 5-3. ビューアーで見る

サーバ起動後にブラウザで `index_osm.html` を開けば自動で読み込まれます
（自動ロード候補に `examples\outputs\realdata\damage_score.geojson` が
含まれています）。

---

## STEP 6. 別のデータで一般的に動かす

`config` YAML を書いて `main.py` に渡すのが本筋です。
`examples\config_synthetic_no_open3d.yaml` をコピーして編集:

```yaml
io:
  pre_las: "D:/path/to/pre.las"
  post_las: "D:/path/to/post.las"
  output_dir: "D:/path/to/output"

crs:
  input_epsg: 6675     # データの座標系
  output_epsg: 4326    # 出力は WGS84 固定
  work_epsg: 6675

preprocess:
  bbox: [minx, miny, maxx, maxy]   # 必要なら切り出し
  voxel_size: 0.5                  # 大規模なら間引き

registration:
  enabled: true                    # ICP 有効化

difference:
  method: "dsm"
  dsm:
    resolution: 0.5
    method: "max"                  # 屋根を見るなら max、地表は min

scoring:
  thresholds: [0.3, 1.0, 3.0]

aggregation:
  unit: "grid"
  grid:
    cell_size: 5.0
```

実行:

```powershell
python main.py --config path\to\your_config.yaml -v
```

`--step preprocess|registration|difference` で部分実行も可能。

---

## STEP 7. 出力の取り扱い

パイプラインが出すもの:

| ファイル | 内容 |
|---|---|
| `damage_score.geojson` | **本命の成果物**。WGS84、ビューアーで開く |
| `dsm_diff.tif` | DSM 差分ラスタ (GeoTIFF)。QGIS で開ける |
| `icp_transform.txt` | ICP の 4×4 変換行列 |

ビューアーで自動ロードさせたい場合は、生成された GeoJSON を
`viewer\` フォルダにコピーしてください:

```powershell
copy examples\outputs\realdata\damage_score.geojson viewer\damage_score.geojson
```

---

## STEP 8. 仮想環境の停止 / 再開

### 終了

```powershell
deactivate
```

### 次回再開

```powershell
cd D:\projects\gensai\las_diff_pipeline
.\.venv\Scripts\Activate.ps1
```

依存関係はそのまま残っているので、すぐ実行できます。

---

## トラブルシューティング

| 症状 | 対処 |
|---|---|
| `python が見つからない` | Python インストール時に PATH に追加されていない。再インストールで「Add to PATH」チェック |
| `pip install` で `Microsoft Visual C++ 14.0 is required` | <https://visualstudio.microsoft.com/visual-cpp-build-tools/> から Build Tools をインストール |
| `geopandas` / `rasterio` で wheel エラー | Python 3.11 などに切り替え、または `pip install --only-binary=:all: geopandas rasterio` |
| `open3d` インストール失敗 | 諦めて該当行をコメントアウト → scipy フォールバックが効く |
| ブラウザでマップが真っ白 | F12 で Console を確認。CORS エラーなら `python -m http.server` で配信しているか確認 |
| GeoJSON が読まれない | `viewer\` フォルダにファイルがあるか確認。なければ「ファイル選択」ボタンから手動指定 |
| ICP が遅い / 終わらない | `registration.enabled: false` にして無効化、または `voxel_size` を大きく (0.5→1.0m) |
| メモリ不足 | `preprocess.bbox` で範囲を絞る、`voxel_size` を大きくする |
| `Operation not permitted` (ファイル削除) | Windows のファイルロック。エクスプローラーで削除するか OS 再起動後に削除 |

---

## よくある質問

### Q. 仮想環境を使わずグローバルに入れていい？
A. 可能ですが、他のプロジェクトと依存関係が衝突しがちなので venv 推奨です。
グローバルに入れる場合は `pip install --user -r requirements.txt`。

### Q. データの座標系がわからない
A. 次のコマンドで確認できます:
```powershell
python -c "import laspy; print(laspy.open('your.las').header.parse_crs())"
```

### Q. 大規模データで重い
A. 順に試してください:
1. `preprocess.bbox` で範囲を絞る
2. `preprocess.voxel_size` を大きく (0.5→1.0→2.0m)
3. `difference.dsm.resolution` を粗く (0.5→1.0→2.0m)
4. `aggregation.grid.cell_size` を大きく (5→10→20m)
5. ICP を `registration.enabled: false` で無効化

### Q. 結果が想定と違う
A. 以下を確認:
- PRE / POST の座標系が一致しているか
- ジオイド高補正の必要性（標高 vs 楕円体高で 30〜40m ずれることがある）
- PRE と POST の点の意味（全点 / ground filtered）が揃っているか
- DSM method (`max` vs `min`) がデータに合っているか

### Q. github にアップしたい
A. `.gitignore` を `D:\projects\gensai\.gitignore` に同梱しています
（`.venv`, `__pycache__`, `*.las`, `outputs/` などを除外）。
```powershell
cd D:\projects\gensai
git init
git add .
git commit -m "initial"
```
で開始できます。大容量 LAS は除外されているので、データは別途配置してください。

### Q. パスを別の場所に変えたい
A. プロジェクトをフォルダごと移動した後、以下を編集:
- `examples\run_realdata.py` の `OUT_DIR`（既定: `D:\projects\gensai\...`）
- 各 YAML の `output_dir`（既定: 相対パス、安全）

YAML 内のパスは config 相対のため、フォルダ移動だけなら基本そのままで動きます。

---

## 参考リンク

- プロジェクト README: `D:\projects\gensai\las_diff_pipeline\README.md`
- 例の README: `D:\projects\gensai\las_diff_pipeline\examples\README.md`
- ビューアー README: `D:\projects\gensai\las_diff_pipeline\viewer\README.md`

---

## 1 分早見表（合成データ → ブラウザ表示まで）

```powershell
cd D:\projects\gensai\las_diff_pipeline
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python examples\generate_synthetic.py
python examples\generate_synthetic_buildings.py
python main.py --config examples\config_synthetic_no_open3d.yaml
python -m http.server 8000
# 別タブで http://localhost:8000/viewer/index_osm.html を開く
```
