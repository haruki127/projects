# システム仕様書

## システム名
点群データ変化解析・危険度マッピングシステム (tengun)

## 目的
地震後の移動・避難所間移動・病院への移動を安全に行えるよう、  
被災前後の点群データを比較して危険箇所を地図上に可視化する。

## 機能
1. LAS形式の点群データ（災害前・後）をブラウザからアップロード
2. 2Dグリッド単位で高さ変化（ΔZ）を計算し変化量を算出
3. 危険度を1〜10段階で評価（10が最高危険）
4. 評価結果を地図（Leaflet.js）上にカラーマッカーで表示

## 技術スタック
| 役割 | 技術 |
|------|------|
| バックエンド | Python 3.x + Flask |
| フロントエンド | HTML / JavaScript / Leaflet.js |
| 点群処理 | laspy, numpy |
| データ形式 | LAS形式 (.las / .laz) |

## 危険度算出ロジック
- 2D グリッド（デフォルト 1m×1m）に点群を分割
- 各セルの前後の平均Z値の差（ΔZ）を計算
- ΔZ を min-max 正規化後、1〜10段階に変換
- GeoJSONとしてフロントエンドに返却

## 入出力
- 入力: LAS ファイル x2（災害前・災害後）
- 出力: 危険度マップ（地図上にポイント表示）

## 制約
- 各コードファイルは最大150行
- 座標系: LASファイルのXY座標を緯度経度として扱う（または適宜変換）
